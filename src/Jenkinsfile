pipeline {
    agent any
    stages {
        stage('Prep') {
            steps {
                    echo '========================================'
                    /* dotnet --version;*/
                    sh "dotnet --version";
                    bat "dotnet --version";
            }
        }
        stage('Build') {
            steps {
                sh "dotnet --version"
                sh "dotnet build /src -o output"
            }
        }
        stage('Test') {
            steps {
                sh "dotnet test /tests"
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}